name: Generate Podcast Feeds
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v5 # https://github.com/actions/checkout
      - name: Run Feed Generator
        uses: varona413/podcast-generator@main
